(module date typed/racket/base/no-check
   (#%module-begin
    (require soft-contract/fake-contract
             (lib "racket/contract/base.rkt")
             (lib "racket/base.rkt")
             (lib "racket/contract.rkt")
             (submod "core-structs.rkt" #%type-decl "..")
             (submod "gregor-structs.rkt" #%type-decl ".."))
    (define g57 exact-nonnegative-integer?)
    (define g58 (or/c g57))
    (define g59 (lambda (x) (equal? x '1)))
    (define g60 (lambda (x) (equal? x '2)))
    (define g61 (lambda (x) (equal? x '3)))
    (define g62 (lambda (x) (equal? x '4)))
    (define g63 (lambda (x) (equal? x '5)))
    (define g64 (lambda (x) (equal? x '6)))
    (define g65 (lambda (x) (equal? x '7)))
    (define g66 (lambda (x) (equal? x '8)))
    (define g67 (lambda (x) (equal? x '9)))
    (define g68 (lambda (x) (equal? x '10)))
    (define g69 (lambda (x) (equal? x '11)))
    (define g70 (lambda (x) (equal? x '12)))
    (define g71 (or/c g59 g60 g61 g62 g63 g64 g65 g66 g67 g68 g69 g70))
    (define g72 (lambda (x) (Date? x)))
    (define g73 string?)
    (define g74 exact-integer?)
    (define g75 (or/c g74))
    (define g76 (lambda (x) (YMD? x)))
    (define g77 '#t)
    (define g78 '#f)
    (define g79 (or/c g77 g78))
    (define generated-contract47
      (case->
       (-> g58 (values g72))
       (-> g58 g71 (values g72))
       (-> g58 g71 g58 (values g72))))
    (define generated-contract48 (-> g72 (values g58)))
    (define generated-contract49 (-> g72 (values g58)))
    (define generated-contract50 (-> g72 (values g73)))
    (define generated-contract51 (-> g72 (values g75)))
    (define generated-contract52 (-> g72 (values g76)))
    (define generated-contract53 (-> g72 g72 (values g79)))
    (define generated-contract54 (-> g72 g72 (values g79)))
    (define generated-contract55 (-> g75 (values g72)))
    (define generated-contract56 (-> g76 (values g72)))
    (module require/contracts racket/base
      (require soft-contract/fake-contract)
      (provide))
    (require (prefix-in -: (only-in 'require/contracts))
             (except-in 'require/contracts))
    (define-values () (values))
    (void)
    (require require-typed-check
             (only-in racket/math exact-round)
             (only-in racket/format ~r)
             "core-adapter.rkt"
             "gregor-adapter.rkt"
             racket/match)
    (begin (require "ymd.rkt") (void))
    (: date-equal-proc (-> Date Date Boolean))
    (define (date-equal-proc x y) (= (Date-jdn x) (Date-jdn y)))
    (: date-hash-proc (-> Date (-> Integer Integer) Integer))
    (define (date-hash-proc x fn) (fn (Date-jdn x)))
    (: date-write-proc (-> Date Output-Port Any Void))
    (define (date-write-proc d out mode)
      (fprintf out "#<date ~a>" (date->iso8601 d)))
    (: date? (-> Any Boolean))
    (define date? Date?)
    (: date (->* (Natural) (Month Natural) Date))
    (define (date y (m 1) (d 1))
      (: ymd YMD)
      (define ymd (YMD y m d))
      (Date ymd (ymd->jdn ymd)))
    (: date->ymd (-> Date YMD))
    (define date->ymd Date-ymd)
    (: date->jdn (-> Date Integer))
    (define (date->jdn d) (Date-jdn d))
    (: ymd->date (-> YMD Date))
    (define (ymd->date ymd) (match-define (YMD y m d) ymd) (date y m d))
    (: jdn->date (-> Integer Date))
    (define (jdn->date jdn) (Date (jdn->ymd jdn) jdn))
    (: date->iso-week (-> Date Natural))
    (define (date->iso-week d) (car (date->iso-week+wyear d)))
    (: date->iso-wyear (-> Date Natural))
    (define (date->iso-wyear d) (cdr (date->iso-week+wyear d)))
    (: date->iso-week+wyear (-> Date (Pairof Natural Natural)))
    (define (date->iso-week+wyear d)
      (define ymd (date->ymd d))
      (define yday (ymd->yday ymd))
      (define iso-wday (jdn->iso-wday (date->jdn d)))
      (match-define (YMD y _ _) ymd)
      (define w (quotient (+ yday (- iso-wday) 10) 7))
      (cond
       ((zero? w)
        (define y-1
          (let ((r (sub1 y)))
            (unless (index? r) (error "date->iso-week+year"))
            r))
        (cons (iso-weeks-in-year y-1) y-1))
       ((and (= w 53) (> w (iso-weeks-in-year y))) (cons 1 (add1 y)))
       ((index? w) (cons w y))
       (else (error "date->iso-week+year"))))
    (: date->iso8601 (-> Date String))
    (define (date->iso8601 d)
      (: f (-> Integer Natural String))
      (define (f n len) (~r n #:min-width len #:pad-string "0"))
      (match
       (Date-ymd d)
       ((YMD y m d) (format "~a-~a-~a" (f y 4) (f m 2) (f d 2)))))
    (: date=? (-> Date Date Boolean))
    (define (date=? d1 d2) (= (date->jdn d1) (date->jdn d2)))
    (: date<=? (-> Date Date Boolean))
    (define (date<=? d1 d2) (<= (date->jdn d1) (date->jdn d2)))
    (provide)
    (provide (contract-out (date<=? generated-contract53))
             (contract-out (date=? generated-contract54))
             (contract-out (date->iso8601 generated-contract50))
             (contract-out (date->iso-wyear generated-contract49))
             (contract-out (date->iso-week generated-contract48))
             (contract-out (jdn->date generated-contract55))
             (contract-out (ymd->date generated-contract56))
             (contract-out (date->jdn generated-contract51))
             (contract-out (date->ymd generated-contract52))
             (contract-out (date generated-contract47)))))
